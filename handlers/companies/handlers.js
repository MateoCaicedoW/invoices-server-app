const { createCompany } = require("./create");
const hasCompany = require("./has_company");
const allCompaniesByUser = require("./all_companies_by_user");

module.exports = {
    createCompany,
    hasCompany,
    allCompaniesByUser
}